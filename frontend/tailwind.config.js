// frontend/tailwind.config.js (Create this file)
/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Scan all relevant files in src
    ],
    theme: {
      extend: {}, // You can add custom theme values here later if needed
    },
    plugins: [],
  }